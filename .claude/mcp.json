{
  "mcpServers": {
    "postgres-klima": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://postgres:master@localhost:5432/klima"
      ],
      "description": "PostgreSQL MCP server for klima database (PostGIS enabled)"
    },
    "puppeteer": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-puppeteer"
      ],
      "description": "Chrome DevTools MCP server for UI development and testing"
    }
  }
}
