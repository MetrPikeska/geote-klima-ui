# ============================================
# GEOTE Climate UI - Environment Configuration
# ============================================
# Copy this file to .env and configure for your environment:
#   cp .env.example .env
#
# SECURITY: Never commit .env to version control!
# ============================================
 cd /home/petr-mikeska/projects/portfolio/geote-klima-ui/backend && cp .env.example .env && echo "✓ Soubor .env vytvořen"
 cd /home/petr-mikeska/projects/portfolio/geote-klima-ui/pg-featureserv && wget -q https://github.com/CrunchyData/pg_featureserv/releases/download/v1.3.1/pg_featureserv_1.3.1_linux_amd64.tar.gz && echo "✓ pg_featureserv stažen"

# ===== DATABASE CONFIGURATION =====
# PostgreSQL + PostGIS connection settings

# Database host
# - Use 'localhost' for local development
# - Use IP address or hostname for remote servers (e.g., 192.168.34.11)
DB_HOST=localhost

# Database port (default PostgreSQL port is 5432)
DB_PORT=5432

 cd /home/petr-mikeska/projects/portfolio/geote-klima-ui/backend && ls -la .env && cat .env | head -5
# Database user (must have SELECT permissions on spatial tables)
DB_USER=postgres

# Database password (REQUIRED - change this!)
DB_PASSWORD=your_password_here

# Database name (must contain PostGIS extension)
DB_NAME=klima

# ===== SERVER CONFIGURATION =====

# Node.js backend port (API endpoint)
# Frontend expects this to be available at http://localhost:4000
PORT=4000

# Node environment
# Options: development | production
# - development: enables verbose logging
# - production: optimized for deployment
NODE_ENV=development

# ============================================
# SETUP INSTRUCTIONS
# ============================================
 pwd
 cp backend/.env.example backend/.env
 test -f backend/.env && echo "EXISTUJE" || echo "NEEXISTUJE"
 cp backend/.env.example backend/.env && ls backend/.env
 git status
 git add .gitignore README.md backend/.env.example backend/db.js LINUX_MIGRATION.md NEXT_STEPS.md PG_FEATURESERV_LINUX.md start.sh stop.sh
 git add .gitignore README.md backend/.env.example backend/db.js LINUX_MIGRATION.md NEXT_STEPS.md PG_FEATURESERV_LINUX.md start.sh stop.sh
 cd /home/petr-mikeska/projects/portfolio/geote-klima-ui && git commit -m "Linux migration: professional startup scripts, enhanced documentation and configuration

✨ New files:
- start.sh - Bash script for service startup with health checks
- stop.sh - Graceful shutdown with PID management
- PG_FEATURESERV_LINUX.md - Comprehensive Linux setup guide
- LINUX_MIGRATION.md - Summary of all changes
- NEXT_STEPS.md - Deployment checklist

🔧 Enhanced files:
- README.md - Complete rewrite (Linux-first, portfolio quality)
- backend/db.js - Refactored with health checks and validation
- backend/.env.example - Comprehensive documentation
- .gitignore - Improved patterns (logs/, *.pid, *.bak)

📊 Technical improvements:
- Environment variable validation
- PostgreSQL + PostGIS health checks
- Connection pooling with timeouts
- Graceful shutdown handlers
- Color-coded terminal output
- Automatic browser opening
- PID-based process management
- Systemd service examples
- Nginx reverse proxy config
- Production deployment guides

🎯 Portfolio ready: professional documentation, security, Linux deployment"
 git push
 cd /home/petr-mikeska/projects/portfolio/geote-klima-ui/pg-featureserv && tar -xzf pg_featureserv_1.3.1_linux_amd64.tar.gz && chmod +x pg_featureserv && rm pg_featureserv_1.3.1_linux_amd64.tar.gz && ls -lh pg_featureserv
 ls -la /home/petr-mikeska/projects/portfolio/geote-klima-ui/backend/.env 2>&1
# 1. Copy this file:
#      cp .env.example .env
#
# 2. Edit .env and set your PostgreSQL password:
#      DB_PASSWORD=your_actual_password
#
# 3. If using remote PostgreSQL, update DB_HOST:
#      DB_HOST=192.168.x.x
#
# 4. Ensure PostgreSQL has PostGIS extension:
#      psql -U postgres -d klima -c "CREATE EXTENSION IF NOT EXISTS postgis;"
#
# 5. Verify connection:
#      node backend/server.js
#      # Should see: "✓ Database connected successfully"
# ============================================
